apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: mentorify
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: mentorify
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8800
      targetPort: http
      protocol: TCP
