apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Namespace
  - base/namespace.yaml

  # Configuration
  - config/

  # Applications
  - apps/backend/
  - apps/frontend/

  # Networking (Istio + NetworkPolicy)
  - networking/
